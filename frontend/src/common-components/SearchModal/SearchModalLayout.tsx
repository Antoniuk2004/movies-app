import {LayoutProps} from "@/types/LayoutProps";

const SearchModalLayout = ({children}: LayoutProps) => {
    return (
        <div className={""}>
            {children}
        </div>
    )
}

export default SearchModalLayout;