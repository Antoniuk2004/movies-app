import {LayoutProps} from "@/types/LayoutProps";

const CountValue = ({children}: LayoutProps) => {
    return (
        <div className={""}>
            {children}
        </div>
    )
}

export default CountValue;